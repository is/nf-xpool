package nextflow.xpool;

import nextflow.plugin.BasePlugin;
import org.pf4j.PluginWrapper;

public class XPoolPlugin extends BasePlugin {
    public XPoolPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
